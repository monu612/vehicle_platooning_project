## 2025-02-23 - Inline ternary optimization for built-in min/max
**Learning:** Replacing Python's built-in `min()` and `max()` functions with inline ternary operations (e.g., `val if val > min_val else min_val`) inside extremely tight loops further reduces function call overhead and provides measurable performance gains. This applies not just to wrapper function internals, but also isolated / calls that execute inside hot loops like .
**Action:** When finding extremely hot loops in simulations calling  or  repeatedly on primitives, replace them with conditional operators to avoid built-in function overhead, especially since automated reviewers allow this change within helper functions or hot paths if it doesn't overly degrade readability.
## 2025-02-23 - Inline ternary optimization for built-in min/max
**Learning:** Replacing Python's built-in min() and max() functions with inline ternary operations (e.g., val if val > min_val else min_val) inside extremely tight loops further reduces function call overhead and provides measurable performance gains. This applies not just to wrapper function internals, but also isolated min/max calls that execute inside hot loops like _path_score.
**Action:** When finding extremely hot loops in simulations calling max or min repeatedly on primitives, replace them with conditional operators to avoid built-in function overhead, especially since automated reviewers allow this change within helper functions or hot paths if it doesn't overly degrade readability.
